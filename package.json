{
  "name": "projects",
  "private": true,
  "scripts": {
    "prepare": "pnpm --filter ./packages/semantic-commit-to-changeset run build && husky install",
    "build": "pnpm recursive run build",
    "test": "vitest"
  },
  "devDependencies": {
    "@changesets/cli": "^2.23.0",
    "@qlonik/semantic-commit-to-changeset": "workspace:*",
    "husky": "^7.0.4",
    "prettier": "~2.5.1",
    "vite": "^2.9.13",
    "vitest": "^0.16.0"
  }
}
